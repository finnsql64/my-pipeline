/* Requires the Docker Pipeline plugin */
Jenkinsfile (Dòng khai báo)
pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                bat 'set'
            }
        }
    }
}
